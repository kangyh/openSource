<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heepay.manage.modules.notification.dao.NotificationEmailDao">
    
	<sql id="notificationEmailColumns">
		a.id AS "id",
		a.email AS "email",
		a.name AS "name",
		a.status AS "status",
		a.create_user AS "createUser",
		a.update_user AS "updateUser",
		a.create_time AS "createTime",
		a.update_time AS "updateTime"
	</sql>
	
	<sql id="notificationEmailJoins">
	</sql>
    
	<select id="get" resultType="NotificationEmail">
		SELECT 
			<include refid="notificationEmailColumns"/>
		FROM notification_email_list a
		<include refid="notificationEmailJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="NotificationEmail">
		SELECT 
			<include refid="notificationEmailColumns"/>
		FROM notification_email_list a
		<include refid="notificationEmailJoins"/>
		<where>
			
			<if test="email != null and email != ''">
				AND a.email LIKE
					<if test="dbName == 'oracle'">'%'||#{email}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{email}+'%'</if>
					<if test="dbName == 'mysql'">concat('%',#{email},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="NotificationEmail">
		SELECT 
			<include refid="notificationEmailColumns"/>
		FROM notification_email_list a
		<include refid="notificationEmailJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO notification_email_list(
			id,
			email,
			name,
			status,
			create_user,
			update_user,
			create_time,
			update_time
		) VALUES (
			#{id},
			#{email},
			#{name},
			#{status},
			#{createUser},
			#{updateUser},
			#{createTime},
			#{updateTime}
		)
	</insert>
	
	<update id="update">
		UPDATE notification_email_list SET 	
			email = #{email},
			name = #{name},
			status = #{status},
			create_user = #{createUser},
			update_user = #{updateUser},
			update_time = #{updateTime}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM notification_email_list
		WHERE id = #{id}
	</update>

	<update id="status">
		UPDATE notification_email_list SET
			status = #{status},
			update_user = #{updateUser},
			update_time = #{updateTime}
		WHERE id = #{id}
	</update>

	<select id="queryList" resultType="java.lang.String">
		SELECT
		email
		FROM
		notification_email_list
		WHERE
		status = 'ENABLE'
	</select>
</mapper>