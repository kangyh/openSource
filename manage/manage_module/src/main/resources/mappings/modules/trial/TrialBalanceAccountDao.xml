<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heepay.manage.modules.trial.dao.TrialBalanceAccountDao">
    
	<sql id="trialBalanceAccountColumns">
		a.id AS "id",
		a.account_id AS "accountId",
		a.account_name AS "accountName",
		a.log_id AS "logId",
		a.balance_amount AS "balanceAmount",
		a.next_log_id AS "nextLogId",
		a.next_arose_amount AS "nextAroseAmount",
		a.next_balance_amount AS "nextBalanceAmount",
		a.next_balance_direction AS "nextBalanceDirection",
		a.next_arose_time AS "nextAroseTime",
		a.must_balance_amount AS "mustBalanceAmount",
		a.account_title AS "accountTitle",
		a.create_time AS "createTime",
		a.update_time AS "updateTime",
		a.status AS "status"
	</sql>
	
	<sql id="trialBalanceAccountJoins">
	</sql>
    
	<select id="get" resultType="TrialBalanceAccount">
		SELECT 
			<include refid="trialBalanceAccountColumns"/>
		FROM trial_balance_account a
		<include refid="trialBalanceAccountJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="TrialBalanceAccount">
		SELECT 
			<include refid="trialBalanceAccountColumns"/>
		FROM trial_balance_account a
		<include refid="trialBalanceAccountJoins"/>
		<where>
			
			<if test="id != null and id != ''">
				AND a.id = #{id}
			</if>
			<if test="accountId != null and accountId != ''">
				AND a.account_id = #{accountId}
			</if>
			<if test="logId != null and logId != ''">
				AND a.log_id = #{logId}
			</if>
			<if test="nextLogId != null and nextLogId != ''">
				AND a.next_log_id = #{nextLogId}
			</if>
		</where>

		ORDER BY create_time DESC

	</select>
	
	<select id="findAllList" resultType="TrialBalanceAccount">
		SELECT 
			<include refid="trialBalanceAccountColumns"/>
		FROM trial_balance_account a
		<include refid="trialBalanceAccountJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO trial_balance_account(
			id,
			account_id,
			account_name,
			log_id,
			balance_amount,
			next_log_id,
			next_arose_amount,
			next_balance_amount,
			next_balance_direction,
			next_arose_time,
			must_balance_amount,
			account_title,
			create_time,
			update_time
		) VALUES (
			#{id},
			#{accountId},
			#{accountName},
			#{logId},
			#{balanceAmount},
			#{nextLogId},
			#{nextAroseAmount},
			#{nextBalanceAmount},
			#{nextBalanceDirection},
			#{nextAroseTime},
			#{mustBalanceAmount},
			#{accountTitle},
			#{createTime},
			#{updateTime}
		)
	</insert>
	
	<update id="update">
		UPDATE trial_balance_account SET 	
			status = 'FINISH'
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM trial_balance_account
		WHERE id = #{id}
	</update>
	
</mapper>