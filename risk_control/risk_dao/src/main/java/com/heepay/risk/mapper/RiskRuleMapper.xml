<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heepay.risk.dao.RiskRuleMapper">
   <select id="getRuleList"  resultType="com.heepay.risk.entity.RiskRule"  parameterType="java.lang.String" >
    select  rule_id, risk_code, risk_name, scope, scope_type, trigger_type, monitor, level, handle_type, 
    status, create_author, create_time, update_author, update_time, remark from risk_rule
    where status = 'ENABLE' and trigger_type=#{triggerType,jdbcType=VARCHAR} order by level desc
  </select>
</mapper>